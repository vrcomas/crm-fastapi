# crm-fastapi

Interfaz de CRM con FastAPI y una vista de dashboard más limpia y espaciada.

## Ejecutar

```bash
pip install fastapi uvicorn jinja2
uvicorn app.main:app --reload
```

Abre: `http://127.0.0.1:8000`
